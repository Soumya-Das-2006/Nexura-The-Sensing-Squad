# apps/notifications/api_urls.py
# Notification API is internal only — no public REST endpoints.
# WhatsApp webhook is mounted separately via whatsapp_urls.py.
from django.urls import path

urlpatterns = []
